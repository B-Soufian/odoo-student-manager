<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- 1. Form View -->
    <record id="view_student_form" model="ir.ui.view">
        <field name="name">school.student.form</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <form string="Student">
                <sheet>
                <header>
                    <button name="action_return_to_list" string="Add Student " type="object"  class="btn-secondary"/>
                </header>

                    <div class="oe_title">
                        <h1>
                            <field name="name" />
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="age"/>
                            <field name="gender"/>
                        </group>
                        <group>
                            <field name="email"/>
                            <field name="phone"/>
                            <field name="enrollment_date"/>
                            <field name="active" invisible="1"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- 2. List View -->
    <record id="view_student_tree" model="ir.ui.view">
        <field name="name">school.student.tree</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <list string="Students">
                <field name="name"/>
                <field name="gender"/>
                <field name="age"/>
                <field name="email"/>
                <field name="enrollment_date"/>
            </list>
        </field>
    </record>

    <!-- 3. Action -->
    <record id="action_school_student" model="ir.actions.act_window">
        <field name="name">Students</field>
        <field name="res_model">school.student</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- 4. Menus -->
    <menuitem id="menu_school_root" name="School" sequence="10"/>    
    <menuitem id="menu_school_student_master" name="Student Master" parent="menu_school_root" sequence="10"/>    
    <menuitem id="menu_school_student" name="Students" parent="menu_school_student_master" action="action_school_student" sequence="10"/>

</odoo>